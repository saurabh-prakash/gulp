---
framework: mocha
coverageThreshold:
  branches: 80
  lines: 80
  functions: 80
  statements: 80
postmerge:
  pattern:
    - "./test/*.js"
  strategy:
    threshold: <5|100s|30m|6h>
    type: <commit|time>
premerge:
  - "./test/*.js"
prerun:
  - npm i
postrun:
  - node --version
version: 2.0
